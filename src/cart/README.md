This folder contains the shared cart implementation.

- `CartProvider.tsx`: React context provider + `useCart` hook
- `cartStorage.ts`: localStorage helpers

`cartStore.tsx` is an older/unused implementation and should be removed once the app is wired to CartProvider.

